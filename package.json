{
  "scripts": {
    "build": "node index.js",
    "dev": "node index.js",
    "start": "nodemon index.js",
    "prod": "npm run build && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-fetch": "^3.3.2"
  }
}
